# Version value below is overwritten during release, but we need to keep
# it in sync for tests to pass.
# Use a simple semantic version without '+' so older parsers that split on
# '.' and parse integers do not choke on the build metadata.
version = 2.7.0
